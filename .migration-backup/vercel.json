{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @workspace/api-server run build && mkdir -p api/_bundle && cp artifacts/api-server/dist/*.mjs api/_bundle/ && pnpm --filter @workspace/olyxee-admin run build:vercel",
  "installCommand": "pnpm install",
  "outputDirectory": "artifacts/olyxee-admin/dist/public",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "includeFiles": "api/_bundle/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
